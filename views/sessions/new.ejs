<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sports Scheduler</title>
    <style>
        body{
            display: flex;
            justify-content: center;
            align-items: center;
            background-color: antiquewhite;
        }
    </style>
</head>
<body>

    <div class="container">

        <nav>
            <a href="/">Dashboard</a> | 
            
        </nav>
        <hr>

        <h1>Create a New Game Session</h1>

        <form action="/sessions" method="POST">
            <div>
                <label for="sport">Choose a sport:</label>
                <select name="sport" id="sport" required>
                    <% if (sports.length > 0) { %>
                        <% sports.forEach(sport => { %>
                            <option value="<%= sport._id %>"><%= sport.name %></option>
                        <% }) %>
                    <% } else { %>
                        <option disabled>No sports available. An admin needs to create one.</option>
                    <% } %>
                </select>
            </div>
            <br>
            <div>
                <label for="venue">Venue:</label>
                <input type="text" id="venue" name="venue" required>
            </div>
            <br>
            <div>
                <label for="dateTime">Date and Time:</label>
                <input type="datetime-local" id="dateTime" name="dateTime" required>
            </div>
            <br>
            <div>
                <label for="playersNeeded">Total Players Needed:</label>
                <input type="number" id="playersNeeded" name="playersNeeded" min="1" required>
            </div>
            <br>
            <button type="submit">Create Session</button>
        </form>

        <hr>

        <a href="/auth/logout">Logout</a>

    </div>

</body>
</html>

